<div class="col-md-4 mx-auto">

    <div class="card">
        <div class="card-body">
            <form action="/empresas/comercios/tags/edit/{{tag.id}}/{{tag.comercio_id}}" method="POST">
                <div class="form-group">
                    <input type="text" name="descripcion" class="form-control" placeholder="Descripcion"
                        value="{{tag.descripcion}}" autofocus>
                </div>
                <div class="form-group">
                    <button class="btn btn-success btn-block">
                        Save
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>